REST API - nodejs e express
    endpoints for registration, add users to database, authentication, get users profile data 


token generation e authentication
    especify wich routes and endpoints the user needed tokens to access
    using passport to create json webtokens 


CORS
    cross origin resource sharing
    backend and frontend are in different ports
    using module cors
    

mongoose ODM
    object document mapper
    make our models, schema (name, username, etc)


angular 2/ angular-cli (command line interface)
    cli generate components, services
    ng build to compile all from frontend


angular router, HTTP module


angular2-JWT
    used to handle with the authentication tokens
    fetch the token from the server and storage in local storage
    check the token to make sure if is valid or expired


auth guard
    for authentication as far the routes
    

angular flash message module
    messages to popup when the user login or logout


compile and deploy
____________________________

modules:
bcrypt: lib to help to hash passwords
bodyparser: parses incoming request bodies